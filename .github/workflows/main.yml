name: Publish Docker images
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Display the path
      run: pwd && ls 
      shell: bash
    - name: Publish to Registry
      uses: elgohr/Publish-Docker-Github-Action@master
      with:
        name: mindspore/ubuntu-gpu
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        snapshot: true
        dockerfile: /home/runner/work/infrastructure/infrastructure/development/jenkins/dockerfile/slaves/ubuntu-gpu/Dockerfile
        workdir: /home/runner/work/infrastructure/infrastructure/development/jenkins/dockerfile/slaves/ubuntu-gpu
